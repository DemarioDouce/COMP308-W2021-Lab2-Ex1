//Development configuration options.
//To sign the session identifier, use a secret string.
module.exports = {
  //Local server for mongodb.
  db: "mongodb://localhost/lab2-ex1",

  sessionSecret: "developmentSessionSecret",
};
